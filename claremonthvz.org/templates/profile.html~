{% extends "base.html" %}

{% block colorScheme %}/media/css/concrete-blood.css{% endblock %}

{% block morehead %} {% endblock %}

{% block greeting %}
  Welcome, {{ first_name|default:"Player" }}!
{% endblock %}

{% block content %}
{% if not_found %}<div id="not_found"> Player information not found. </div>
{% else %}
{% if is_me %}<p> THIS IS YOUR PROFILE, ISN'T IT? </p> {% endif %}
{% if picture != "" %}<img src="/media/{{ picture }}" id="profile_pic">{% endif %}
    {% if hardcore %}
    <h3>Legendary</h3>
    {% endif %}

<table>
    <tr><td>Player:</td><td>{{ first }} {{ last }}</td></tr>
    <tr><td>School:</td><td>{{ school }}</td></tr>
    <tr><td>Graduation Year:</td><td>{{ grad_year }}</td></tr>
    <tr><td>Has Cell:</td><td>{{ using_cell }}</td></tr>
    <tr><td>In Game:</td><td>{{ in_game }}</td></tr>
    <tr><td>Team:</td><td>{{ team }}</td></tr>
    <tr><td>Game Count:</td><td>{{ game_count }}</td></tr>
    <tr><td>Meals:</td><td>{{ meals }}</td></tr>
</table>

{% endif %}
{% endblock %}
